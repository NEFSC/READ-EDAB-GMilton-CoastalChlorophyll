# Reflectance data 

All reflectance (Rrs) data was collected from SeaBASS and GLORIA. Rrs data from SeaBASS is formated in both long format (wavelength in column next to rrs column) and wide format (wavelength value appended to rrs column name). All raw Rrs data is transformed to long format but will eventually be turned to wide format in final data aggregation. 